# ---> Vim
[._]*.s[a-w][a-z]
[._]s[a-w][a-z]
*.un~
Session.vim
.netrwhist
*~
*.snap
actionlint

# Snap Store credentials — never commit (e.g. `snapcraft export-login` output)
login

# Claude Code local state (settings, worktrees, logs)
.claude/

# Local on-device test scripts (one per PR, not part of the build)
.test/
